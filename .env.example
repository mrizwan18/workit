# Web Push (background reminders when app is closed)
# Generate: npm run generate-vapid
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
PUSH_CONTACT_EMAIL=mailto:you@example.com

# Vercel Cron (protect /api/send-due-notifications)
CRON_SECRET=

# Upstash Redis (store push subscriptions)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
